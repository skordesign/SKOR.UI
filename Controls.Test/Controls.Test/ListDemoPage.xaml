<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:c="clr-namespace:Skor.Controls;assembly=Skor.Controls"
             x:Class="Controls.Test.ListDemoPage">
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Items}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <c:MasterDetailView IsDetailShown="{Binding IsShow}" TapMasterToShowDetail="True">
                            <c:MasterDetailView.Master>
                                <Frame HeightRequest="80" >
                                    <Label Text="{Binding Index}"/>
                                </Frame>
                            </c:MasterDetailView.Master>
                            <c:MasterDetailView.Detail>
                                <Frame BackgroundColor="Gray" >
                                    <Label TextColor="White" Text="{Binding Index}" />
                                </Frame>
                            </c:MasterDetailView.Detail>
                        </c:MasterDetailView>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>